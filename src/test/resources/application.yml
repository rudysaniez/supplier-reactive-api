
logging:
  level:
    root: INFO
    '[org.springframework.jdbc.core]': TRACE
    '[org.springframework.transaction]': TRACE
    '[org.springframework.data.r2dbc]': TRACE

okta:
  oauth2:
    client-secret: power-secret
    client-id: power-user
    issuer: https://dev-4118925.okta.com/oauth2/default
    scopes:
    - demo:register
    - demo:monitor
    - openid

spring:
  r2dbc:
    url: r2dbc:h2:mem:///suppliersdb;DB_CLOSE_DELAY=1
    username: sa
    password:
    
  liquibase:
    change-log: classpath:liquibase/create-tables.xml
    enabled: true
    url: jdbc:h2:mem:suppliersdb;DB_CLOSE_DELAY=-1
    user: sa
    password:
    
  data:
    jdbc:
      repositories:
        enabled: false
